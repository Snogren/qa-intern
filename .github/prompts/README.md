Run requirements-analyzer against Jira ticket to ask important questions and generate acceptance criteria.

Run change-analyzer as soon as a PR is opened to review changed files and create summaries.

Run context-analyzer to review output of the other 2 prompts and predict if tests will pass, and recommend final tests.

tpo-orchestrator replaces requirements-analyzer as a multi-agent system.